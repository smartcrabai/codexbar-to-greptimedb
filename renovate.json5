{
  extends: [
    "config:best-practices"
  ],
  dependencyDashboard: false,
  // Scan the central source file for Renovate only in the terraforms repository
  // (additive, so the default patterns are still kept; this path does not
  // exist in the distribution target repositories).
  "github-actions": {
    managerFilePatterns: [
      "/(^|/)environments/shared/cruise/workflow\\.yml$/"
    ],
  },
  minimumReleaseAge: "3 days",
  lockFileMaintenance: {
    minimumReleaseAge: null,
  },
  vulnerabilityAlerts: {
    minimumReleaseAge: null,
  },
  packageRules: [
    {
      matchUpdateTypes: [
        "minor",
        "patch",
        "digest",
        "pinDigest"
      ],
      groupName: "all minor, patch, and digest updates",
      groupSlug: "all-minor-patch-digest-updates",
    },
    {
      matchUpdateTypes: [
        "major"
      ],
      prBodyNotes: [
        "<!-- renovate-major-update -->"
      ],
    },
    {
      matchManagers: [
        "github-actions"
      ],
      minimumReleaseAge: "7 days"
    },
    {
      // setup-swift v2 has a hardcoded Swift version allowlist that breaks on
      // new Swift majors (e.g. "Version 6.4 is not available"). v3 is
      // Swiftly-based with no allowlist, so allow major updates for it.
      matchManagers: [
        "github-actions"
      ],
      matchDepNames: [
        "swift-actions/setup-swift"
      ],
      matchUpdateTypes: [
        "major"
      ],
      enabled: true,
    },
    {
      // digest/pinDigest updates have no releaseTimestamp, so minimumReleaseAge
      // (with the default minimumReleaseAgeBehaviour "timestamp-required") keeps
      // renovate/stability-days pending forever. Must stay last to override the
      // github-actions rule above.
      matchUpdateTypes: [
        "digest",
        "pinDigest"
      ],
      minimumReleaseAge: null,
    }
  ]
}
